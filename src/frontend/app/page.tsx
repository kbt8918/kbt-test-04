import { PrototypeShell } from "@/components/PrototypeShell";

// 안심맵 — 전체 화면 흐름 프로토타입 (클로드 디자인 재구현)
// 상단 플로우 스위처로 로그인·온보딩 / 부모님 / 가족 / 관리자 관점을 전환합니다.
export default function HomePage() {
  return <PrototypeShell />;
}
